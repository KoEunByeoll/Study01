<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.member.dao.MemberDao"> <!--namespace에는 풀패키지 이름 그대로 작성. -->

	<select id="selectListMember" resultType="memberVo">
		SELECT SID as sid, NAME as name, MAJOR as major, SUBJECT as subject FROM MEMBER
	</select>

	<select id="selectOneMember" parameterType="int" resultType="memberVo">
		SELECT SID as sid, NAME as name, MAJOR as major, SUBJECT as subject FROM MEMBER WHERE SID = ${sid}
	</select>

	<delete id="deleteMember" parameterType="memberVo">
		DELETE FROM MEMBER WHERE SID=${sid}
	</delete>
	
	<insert id="insertMember" parameterType="memberVo">
		INSERT INTO MEMBER VALUES(#{sid},#{name},#{major},#{subject})
	</insert>
	
	<update id="updateMember" parameterType="memberVo">
		UPDATE MEMBER set NAME = #{name}, SUBJECT = #{subject}, MAJOR = #{major} WHERE SID=${sid}	
	</update>
</mapper>